name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch: # Allow manual triggers

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment: production # Use GitHub environments for protection

    steps:          
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Test Network Connectivity
        run: |
          # Try to reach the server on defined port with a 5-second timeout
          nc -zv -w 5 ${{ secrets.SSH_HOST }} ${{ secrets.SSH_PORT }}

      - name: Configure SSH for Rsync
        run: |
          mkdir -p ~/.ssh/
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          cat >>~/.ssh/config <<END
          Host vps
            HostName ${{ secrets.SSH_HOST }}
            User ${{ secrets.SSH_USER }}
            Port ${{ secrets.SSH_PORT }}
            IdentityFile ~/.ssh/deploy_key
            StrictHostKeyChecking no
          END

      - name: Deploy using Rsync
        run: |
          rsync -avz --delete \
            --exclude '.git/' \
            --exclude 'node_modules/' \
            --exclude '.env' \
            ./ vps:/${{vars.TARGET_DIR}}/

      
      - name: Execute Install & Build on Remote Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          command_timeout: 30m
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script: |
            
            # Add PNPM to PATH
            export PNPM_HOME="$HOME/.local/share/pnpm"
            export PATH="$PNPM_HOME:$PATH"
            
            # Navigate to project directory
            cd /${{vars.TARGET_DIR}}

            # Verify Node and PNPM exist
            echo "Using Node: $(node -v)"
            echo "Using PNPM: $(pnpm -v)"

            echo "APP_NAME=${{vars.APP_NAME}}"
            echo "TARGET_DIR=${{vars.TARGET_DIR}}"

            
            # Create .env file on VPS using values from Github Actions Secrets
            echo "Creating .env file..."
            echo "NODE_ENV=production" > .env
            echo "APP_NAME=${{vars.APP_NAME}}" >> .env
            echo "TARGET_DIR=${{vars.TARGET_DIR}}" >> .env
            echo "DATABASE_URI=${{ secrets.DATABASE_URI }}" >> .env
            echo "PAYLOAD_SECRET=${{ secrets.PAYLOAD_SECRET }}" >> .env
            echo "NEXT_PUBLIC_SERVER_URL=${{ secrets.NEXT_PUBLIC_SERVER_URL }}" >> .env
            echo "CRON_SECRET=${{ secrets.CRON_SECRET }}" >> .env
            echo "PREVIEW_SECRET=${{ secrets.PREVIEW_SECRET }}" >> .env
            echo "SMTP_PASS=${{ secrets.SMTP_PASS }}" >> .env
            echo "SMTP_USER=${{ secrets.SMTP_USER }}" >> .env
            echo "SMTP_HOST=${{ secrets.SMTP_HOST }}" >> .env

            echo "Running scripts/deploy.sh..."
            chmod +x scripts/deploy.sh
            scripts/deploy.sh
            
            # Install project
            # echo "Installing..."
            # pnpm install --frozen-lockfile

            # Build project
            # echo "Building..."
            # pnpm cross-env NODE_OPTIONS=--no-deprecation rm -rf .next && pnpm build

            # Run PayloadCMS Migrations
            # echo "Running PayloadCMS Migrations..."
            # pnpm payload migrate

            # Restart Application using PM2
            # echo "reload with PM2..."
            # pm2 reload ecosystem.config.cjs --env production --update-env || pm2 start ecosystem.config.cjs --env production --update-env
            
            # # Wait 10 seconds for the app to start
            # echo "Waiting for app to start..."
            # sleep 10
            
            # # Check if the local port 3000 is responding
            # echo "Checking for response from application..."
            
            # if curl --retry-connrefused --retry-delay 5 --retry 3 --fail http://127.0.0.1:3000; then
            #     echo "✅ Deployment Successful: App is responding!"
            # else
            #     echo "❌ Deployment Failed: App is not responding."
            #     # TODO: Revert to previous build or alert??
            #     exit 1
            # fi
            
